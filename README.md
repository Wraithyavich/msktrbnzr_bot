# msktrbnzr_bot

Складские файлы:

- `vz.csv` — Вязовский
- `gar.csv` — Гаражная
- `per.csv` — Перовская

Формат строки склада:

```csv
артикул;доп_артикул;количество;цена;флаг_скидки
```

Если пятый столбец равен `1`, пометка `(скидка)` не выводится.
